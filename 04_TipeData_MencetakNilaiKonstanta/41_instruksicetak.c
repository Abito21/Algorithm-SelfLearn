#include<stdio.h>

void main(){
    int A, B, T;
    A = 5;
    B = 2;
    T = A + B;
    printf("%i", T);    // printf merupakan fungsi dari library stdio.h untuk menampilkan variabel T (dengan tipe data integer) dalam bentuk string.
}