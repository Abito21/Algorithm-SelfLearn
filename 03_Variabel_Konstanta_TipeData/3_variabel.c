#include<stdio.h> # Library

void main(){
    /*
        - int disebut sebagai tipe data integer
        - A / B / T disebut sebagai variabel.
    */
    int A, B, T;
    A = 5;              // Variabel A berisi nilai 5
    B = 2;              // Variabel B berisi nilai 2
    T = A + B;          // Variabel T merupakan penjumlahan dari nilai variabel A dan nilai variabel B
    printf("%i", T);    // printf merupakan fungsi dari library stdio.h untuk menampilkan variabel T (dengan tipe data integer) dalam bentuk string.
}